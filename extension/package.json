{
  "name": "purl-extension",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/zip.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.321",
    "@types/node": "^25.9.1",
    "typescript": "^5",
    "vite": "^6"
  }
}
